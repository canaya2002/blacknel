/**
 * Reserved for ambient declarations. Augment global namespaces here when
 * needed (process.env shapes, third-party module declarations, etc.).
 *
 * Strongly-typed env access lives in `lib/env.ts` (added in Commit 2),
 * not here.
 */
export {};
